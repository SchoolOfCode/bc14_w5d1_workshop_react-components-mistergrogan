// Inside index.js, create a functional component that returns a JSX element for your card.

function NewCard() {
    return (
    <div className="card">This is the new card!</div> //Add a className className="card" to the JSX as there is a className allready applied to it in App.css (you can customize the CSS if you have time!)    
    )
}
export default NewCard;